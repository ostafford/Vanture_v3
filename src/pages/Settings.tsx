export function Settings() {
  return (
    <div>
      <h1 className="mb-4">Settings</h1>
      <p className="text-muted">Settings options (e.g. clear data, re-sync) will be added in a later phase.</p>
    </div>
  )
}
